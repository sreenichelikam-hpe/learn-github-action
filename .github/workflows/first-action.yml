name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps: 
      - name: Print greeting
        run: echo "Welcome to Github actions"
      - name: print goodbye
        run: echo "Done -bye"
